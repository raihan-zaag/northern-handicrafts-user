import React, { Fragment } from "react";
import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default LoginPage;
